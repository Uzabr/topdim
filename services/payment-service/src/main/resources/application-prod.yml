# Production profile — отключает Swagger UI и API docs.
# Активируется через: SPRING_PROFILES_ACTIVE=prod
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# M3: понижаем логирование до INFO в проде
logging:
  level:
    uz.topdim: INFO
