# CODEOWNERS — кто обязан ревьюить какие каталоги
#
# GitHub автоматически добавляет владельца в ревьюеры PR, затрагивающего его пути.
# Работает как жёсткое требование только при включённой опции
# "Require review from Code Owners" в правиле защиты ветки.
#
# Замени @Uzabr и @shoxi0718 на реальные GitHub-логины,
# если они отличаются. Синтаксис: последнее совпавшее правило побеждает,
# поэтому порядок снизу вверх — от общего к частному.
#
# Обновлено 30.07.2026

# ── По умолчанию ─────────────────────────────────────────────────────────────
# Всё, что не описано ниже, ревьюит владелец проекта.
*                                   @Uzabr

# ── Зона Шохисты ─────────────────────────────────────────────────────────────
# Фронтенд админки и партнёрки. Владелец проекта всё равно в ревьюерах:
# он единственный, кто знает контекст, и пока это не изменится — так и остаётся.
/frontend/admin-app/                @shoxi0718 @Uzabr
/frontend/partner/                  @shoxi0718 @Uzabr

# ── Деньги ───────────────────────────────────────────────────────────────────
# Платежи, заказы, погашение купонов. По Definition of Done здесь нужен
# второй ревьюер-человек. Автоматика этого требования не закрывает.
/services/payment-service/          @Uzabr
/services/order-service/            @Uzabr

# ── Аутентификация и права ───────────────────────────────────────────────────
# Ошибка здесь не ломает сборку, а тихо открывает доступ.
/services/identity-service/         @Uzabr
/infrastructure/api-gateway/        @Uzabr

# ── Схема базы данных ────────────────────────────────────────────────────────
# Миграции необратимы в production. Любая правка — только через владельца.
**/src/main/resources/db/migration/ @Uzabr

# ── Инфраструктура и доставка ────────────────────────────────────────────────
# Изменение пайплайна может отключить сам гейт качества.
/.github/                           @Uzabr
/infrastructure/                    @Uzabr
/docker/                            @Uzabr
/docker-compose.yml                 @Uzabr
/docker-compose.prod.yml            @Uzabr
/scripts/                           @Uzabr
/build.gradle                       @Uzabr
/settings.gradle                    @Uzabr

# ── Правила для ИИ-агентов ───────────────────────────────────────────────────
# Тот, кто правит эти файлы, правит критерии собственного ревью.
/CLAUDE.md                          @Uzabr
/AGENTS.md                          @Uzabr
/.claude/                           @Uzabr

# ── Общие модули ─────────────────────────────────────────────────────────────
# Контракты между сервисами: правка ломает всех потребителей сразу.
/shared/                            @Uzabr

# ── Процесс ──────────────────────────────────────────────────────────────────
/docs/scrum/                        @Uzabr